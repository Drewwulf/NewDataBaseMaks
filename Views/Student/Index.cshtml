@model MaksGym.Models.ViewModels.StudentViewModel

@{
    ViewData["Title"] = "Студент";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8 col-sm-12">
            <div class="card shadow-lg border-0 rounded-3">
                <div class="card-header bg-primary text-white text-center">
                    <h3 class="mb-0">Додати студента</h3>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Create" enctype="multipart/form-data" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="NewStudent.UserId" class="form-label fw-bold">Користувач</label>
                            <select asp-for="NewStudent.UserId" class="form-select">
                                <option value="">-- Виберіть користувача --</option>
                                @foreach (var user in Model.Users)
                                {
                                    <option value="@user.Id">@user.FullName</option>
                                }
                            </select>
                            <span asp-validation-for="NewStudent.UserId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Фото</label>
                            <input type="file" name="PhotoFile" class="form-control" />
                        </div>

                        <div class="d-flex justify-content-between">
                            <button type="submit" class="btn btn-success px-4">
                                <i class="bi bi-plus-circle"></i> Додати
                            </button>
                            <a asp-action="Index" asp-controller="Home" class="btn btn-outline-secondary px-4">
                                <i class="bi bi-arrow-left"></i> Назад
                            </a>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>

    <div class="row mt-5 mb-3">
        <div class="col-md-6">
            <input type="text" id="searchInput" class="form-control" placeholder="Пошук студента..." />
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm border-0 rounded-3">
                <div class="card-header bg-light">
                    <h4 class="mb-0">Список студентів</h4>
                </div>
                <div class="card-body p-3">
                    <div class="table-responsive">
                        <table id="trainersTable" class="table table-striped table-hover align-middle">
                            <thead class="table-dark">
                                <tr>
                                    <th scope="col">Id</th>
                                    <th scope="col">Користувач</th>
                                    <th scope="col">Фото</th>
                                    <th scope="col" class="text-center">Дії</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var student in Model.students)
                                {
                                    if (!student.IsDeleted)
                                    {
                                        <tr>
                                            <td>@student.StudentId</td>
                                            <td>@student.User.FullName</td>
                                            <td>
                                                @if(student.PhotoPath != null)
                                                {
                                                    <img src="@student.PhotoPath" alt="Фото студента"
                                                    class="rounded-circle shadow-sm"
                                                    style="width: 80px; height: 80px; object-fit: cover;" />
                                                }
                                                else
                                                {
                                                    <img src="/uploads/iconprfile.png" alt="Фото студента"
                                                         class="rounded-circle shadow-sm"
                                                         style="width: 80px; height: 80px; object-fit: cover; background-color:darkgray" />
                                                }

                                            </td>
                                            <td class="text-center">
                                              
                                               
                                                <a asp-action="Delete" asp-route-id="@student.StudentId"
                                                   class="btn btn-outline-danger btn-sm"
                                                   onclick="return confirm('Ви впевнені, що хочете видалити цього студента?');">
                                                    <i class="bi bi-trash"></i> Видалити
                                                </a>
                                                <a asp-action="Details" asp-route-id="@student.StudentId"
                                                   class="btn btn-outline-info btn-sm">
                                                    <i class="bi bi-trash"></i> Деталі
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        document.getElementById("searchInput").addEventListener("keyup", function () {
            let value = this.value.toLowerCase();
            let rows = document.querySelectorAll("#trainersTable tbody tr");

            rows.forEach(row => {
                let text = row.innerText.toLowerCase();
                row.style.display = text.includes(value) ? "" : "none";
            });
        });
    </script>
}
