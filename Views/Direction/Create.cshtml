@model MaksGym.Models.Direction

@{
    ViewData["Title"] = "Додати напрямок";
}

<h2>Додати напрямок</h2>

<form asp-action="Create" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <div class="form-group">
        <label asp-for="DirectionName" class="control-label"></label>
        <input asp-for="DirectionName" class="form-control" />
        <span asp-validation-for="DirectionName" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="DirectionDescription" class="control-label"></label>
        <textarea asp-for="DirectionDescription" class="form-control"></textarea>
        <span asp-validation-for="DirectionDescription" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Додати</button>
    <a asp-action="Index" class="btn btn-secondary">Назад</a>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
